moveHyphen(char str[],int n);

The function accepts a string “str” of length ‘n’, that contains alphabets and hyphens (-).
Implement the function to move all hyphens(-) in the string to the front of the given string. 
   
   
   
   public static void moveHypen(String str){
        String hypen = "";
        for(int i = 0; i < str.length(); i++){
            if(str.charAt(i) == '-'){
                hypen += str.charAt(i);
        
            }
            
            
        }
        String orgString = str.replace("-", "");
        System.out.print(hypen + orgString);

    }

    public static void main(String[] args) {
        moveHypen("he-ll-o");
    }
